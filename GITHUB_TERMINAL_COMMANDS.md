# GitHub

```bash
cd ~/Downloads/RentalAffordabilityStressMapper_Local
git init
git branch -M main
git add -A
git status
git commit -m "feat: add RentRelief rental affordability stress mapper"
git remote remove origin 2>/dev/null || true
git remote add origin https://github.com/shaunakmirajgaonkar/rental-affordability-stress-mapper.git
git push -u origin main
```
