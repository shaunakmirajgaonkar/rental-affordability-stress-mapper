# Bugfix Notes

Primary neighborhood IDs must be unique. Historical cost records intentionally allow repeated neighborhood IDs across periods. CSV headers are normalized for common aliases. Plotly marker sizes use numeric population. The app avoids fragile DataFrame attribute access.
